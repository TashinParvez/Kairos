<div class="container">
            <div class="slice slice-top">
                <div class="slice-horizontal slice-horizontal-top"></div>
                <div class="slice-horizontal slice-horizontal-bottom"></div>
            </div>
            <div class="slice slice-middle"></div>
            <div class="slice slice-bottom"></div>
        </div>


<style>
        .container {
            display: grid;
            grid-template-rows: 2fr 3fr 1fr;
            /* Divide the container into 3 rows with the specified fractions */
            grid-template-columns: 4fr 1fr;
            /* Divide the container into 2 columns with the specified fractions */
            gap: 10px;
            /* Add some gap between the slices */
        }

        .slice {
            background-color: lightgray;
        }

        .slice-top {
            display: grid;
            grid-template-rows: 4fr 1fr;
            /* Divide the top slice into 2 rows with the specified fractions */
        }

        .slice-horizontal {
            background-color: darkgray;
        }

        .slice-horizontal-top {
            grid-row: 1 / span 1;
            /* Span the first row */
        }

        .slice-horizontal-bottom {
            grid-row: 2 / span 1;
            /* Span the second row */
        }

        .slice-middle {
            /* This slice will take the remaining space */
        }

        .slice-bottom {
            /* This slice will take the remaining space */
        }
    </style>